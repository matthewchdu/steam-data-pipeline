version: 2

sources:
  - name: bronze_dataset
    description: "Bronze tables processed by the spark job"
    database: bronze_dataset 
    schema: prod_dataset
    tables:
      - name: games
      - name: reviews