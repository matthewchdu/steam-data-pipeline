version: 2

sources:
  - name: bronze_dataset
    description: "Bronze tables processed by the spark job"
    database: "REPLACE_WITH_YOUR_PROJECT_ID"
    schema: bronze_dataset
    tables:
      - name: games
      - name: reviews