version: 2

sources:
  - name: bronze_dataset
    description: "Bronze tables processed by the spark job"
    database: steam-data-pipeline-507614 
    schema: bronze_dataset
    tables:
      - name: games
      - name: reviews